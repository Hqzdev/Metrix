# Component Conventions

Components in this directory are shared by web routes and should stay reusable.

## Guidelines

- keep domain-specific copy close to page-level components;
- prefer small typed props over passing large data objects;
- keep layout primitives separate from page composition;
- place browser-only behavior behind explicit client components.
