# Web Workflow

Use this file for web-specific implementation notes that do not belong to route
or component documentation.

## Branch Areas

- `feature/web/apps` — route-level app changes under `apps/web/app`.
- `feature/web/components` — shared UI component changes under `apps/web/components`.
- `feature/web/misc` — configuration, hooks, assets, styles, and supporting web files.
